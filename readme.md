# WGPU solar system

This is my learning project to understand WGPU - modern GPU API

## How to run

**Requirements** - Rust toolchain, `git-lfs`

- Clone entire repo, including images stored using `git-lfs`
- Launch project with `cargo run`